# Les 17 Objectifs de developpement durable (ODD)

Retourne un data.frame des 17 objectifs, avec leur numero et leur titre
officiel (traduction francaise des Nations Unies).

## Usage

``` r
list_sdg_goals()
```

## Value

Un `data.frame` avec les colonnes `goal` et `title`.
